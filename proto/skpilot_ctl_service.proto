syntax = "proto3";

package skpilot_ctl_service;

option go_package = "p9t.io/skafos/pkg/proto";

import "default.proto";

message ApplyRuleRequest {
    bytes rule = 1;
}

service SkpilotCtlService {
    rpc ApplyRule(ApplyRuleRequest) returns(default.DefaultResponse);
}